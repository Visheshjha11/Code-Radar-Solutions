#include <stdio.h>

int main() {
    int num, complement, result;

    scanf("%d", &num);
    scanf("%d", &complement);

    result = num ~ shift;
    printf("%d\n", result);

    return 0;
}
